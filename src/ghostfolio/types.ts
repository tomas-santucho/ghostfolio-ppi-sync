export interface GhostfolioActivity { id?:string; accountId?:string; type?:string; date?:string; symbol?:string; quantity?:number; unitPrice?:number; fee?:number; currency?:string; comment?:string; [key:string]:unknown; }
export interface GhostfolioImportActivity { accountId:string; type:string; date:string; symbol?:string; quantity?:number; unitPrice?:number; fee?:number; currency:string; dataSource?:string; comment?:string; }
export interface GhostfolioImportResult { dryRun:boolean; imported:number; activities:GhostfolioActivity[]; validationFailures?:GhostfolioActivity[]; }
export interface GhostfolioManualAssetProfile {symbol:string;name:string;currency:string;dataSource:'MANUAL';countries:[];holdings:[];isActive:true;marketData:{date:string;marketPrice:number}[];sectors:[];}
export interface GhostfolioClient { getActivities():Promise<GhostfolioActivity[]>; importActivities(activities:GhostfolioImportActivity[],options?:{dryRun?:boolean}):Promise<GhostfolioImportResult>; updateActivity?(activity:GhostfolioImportActivity&{id:string}):Promise<void>; upsertManualAssetProfiles?(profiles:GhostfolioManualAssetProfile[],options?:{dryRun?:boolean}):Promise<void>; }
