export interface PpiTransaction { agreementDate:string; settlementDate?:string; currency:string; amount:number|string; price:number|string; description:string; ticker?:string; quantity:number|string; balance:number|string; externalId?:string; }
export interface PpiOrder { id:number; externalId?:string; instrumentType:string; operation:string; ticker:string; status:string; date:string; settlement:string; quantity:number|string; orderType:string; operationType:string; operationMaxDate?:string|null; price:number|string; currency:string; amount:number|string; }
export interface PpiToken { accessToken:string; refreshToken:string; tokenType:string; expires:number; }
export interface PpiInstrument { ticker:string; description:string; currency:string; type:string; market:string; isin?:string|null; }
export interface PpiPosition {ticker:string;description:string;currency:string;price:number;amount:number;quantity:number;collateralQuantity?:number;isin?:string|null;cajaValoresCode?:string|number|null;ppc?:unknown;}
export interface PpiClient { authenticate():Promise<void>; getTransactions(options:{accountId:string;from?:Date;to?:Date}):Promise<PpiTransaction[]>; getOrders?(options:{accountId:string;from?:Date;to?:Date}):Promise<PpiOrder[]>; getOrderDetail?(options:{accountId:string;orderId:number;externalId?:string}):Promise<PpiOrder>; getAccount(accountId:string):Promise<PpiAccountBalance[]>; getPositions(accountId:string):Promise<PpiPosition[]>; searchInstrument?(ticker:string):Promise<PpiInstrument[]>; }
export interface PpiHolding { name:string; simbol?:string; amount:number; settlement:string; }
export interface PpiAccountBalance { currency:string; availability:PpiHolding[]; }
